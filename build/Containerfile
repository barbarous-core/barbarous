FROM quay.io/fedora/fedora-coreos:stable

# This is a vanilla Fedora CoreOS image.
# No custom users, branding, or services are included.

# Replace minimal NFS tools with the standard version
RUN rpm-ostree override remove nfs-utils-coreos --install nfs-utils \
    && ostree container commit
